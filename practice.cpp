#include <iostream>

// int main is the starting point of the code (to begin with)
int main (){


    // std::cout stands for standard character output which prints the character where "<<" can be the sign of =

    // std::endl stands for standard endline (to begin with a new line)

    std::cout << "I'm panshul i like pizza" << '\n';

    // '\n' can also be used instead of std::endl
    std::cout << "i also have ice cream after it" << std::endl;

    //note- always end the code line with ' ; ' as its like a full stop

        return 0;
        // return 0 just indicates the computer that the task is completed
}